Things in utils should be:
1. generic, not tied to this particular project
2. reusable (which comes from point 1 really)

Anything project-specific should either be moved elsewhere, or
be made configurable/ parametricized in such a way that it is generic
and reusable

